#import "week05_202312328.typ": sourcecode
#set text(font: "Meiryo UI")

#let date = datetime.today().display()
#set page(paper: "a4", margin: 1.5cm)

//ページ番号を設定
#set page(numbering: "1")

//表のキャプションを上に設定
#show figure.where(kind: table): set figure.caption(position: top)


#align(center, text(20pt)[
  //タイトル
  応用プログラミング 第6週課題 応用プログラム開発２
])

#align(center, text(12pt)[
  名前: 今泉志常 #h(1cm) 学籍番号: 202312328#h(1cm) 学類: 工学システム学類\
  提出日: #date
])

= 基本機能

== 使い方
Space キーで化学スラスターを噴射し、推進力を得て移動する。最大速度が設定されており、過度な加速は制限される。
A/D キー（または矢印キー）で電気スラスターを使用し、機体の姿勢制御（回転）を行う。
慣性が働く宇宙空間での操作となるため、減速や方向転換には逆方向への推力が必要となる。

== UI（画面）の基本構成
画面左上に、電気スラスターの使用可能量を表す「バッテリーゲージ」を表示する。
また、同画面左上に、スタート地点からの進行距離を表示するUIを配置する予定である。

== オブジェクトの振る舞い
プレイヤーは物理演算に基づき、慣性を持った浮遊挙動を行う。
画面外に出た場合、イベントが発火しゲームオーバーとなる判定処理を行う。
今後は、デブリや隕石などの障害物を配置し、これらに衝突した場合もゲームオーバーとなる仕組みを実装する。

= 進捗状況

== 実装内容
今週は、プレイヤーキャラクターの基本的な制御スクリプト `SatelliteController.cs` を実装し、以下の挙動を確認した。
- Spaceキーによる加速（化学スラスター）
- A/Dキーによる姿勢制御（電気スラスター）とバッテリー消費
- 慣性による等速直線運動
- 画面外への逸脱判定とゲームオーバー処理

#figure(
  image("image.png", width: 80%),
  caption: [現在のプレイヤー挙動（慣性移動と回転）],
)

= 計画の修正

現在、基本的な移動操作は実現できたが、ゲームとしての体を成すためには障害物の回避やスコア要素（進行距離）の実装が不可欠である。
次回以降、以下の手順で開発を進める予定である。
1. プレイヤーの見た目の変更（宇宙船らしいアセットの導入）
2. 障害物（デブリ）の配置と当たり判定の実装
3. バッテリー回復システム（太陽光パネルによる充電など）の検討
4. UI（バッテリー残量、距離）の実装

= 使い方の導線

1. アプリを起動すると、宇宙空間にプレイヤーが表示される。
2. スペースキーとA/Dキーで推進・回転を行い、障害物を回避し続ける。
3. バッテリーが尽きる、画面外に出る、または障害物に衝突するとゲームオーバーとなり、リトライ画面へ遷移する。
